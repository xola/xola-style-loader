{
  "name": "xola-style-loader",
  "version": "0.1.1",
  "description": "Webpack loader for Xola web apps that injects convention based style files into views",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Xola",
  "devDependencies": {
    "eslint": "^7.12.1"
  },
  "dependencies": {
    "path": "^0.12.7"
  }
}
